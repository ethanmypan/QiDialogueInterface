# MongoDB Connection String
# Get this from your MongoDB Atlas dashboard or your lab's MongoDB instance
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>
MONGODB_URI=mongodb://localhost:27017/dialogue-editor

# Server Port (optional, defaults to 3000)
PORT=3000
